{
  "name": "wintermaze",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A maze tower defense in the lineage of the Warcraft III custom games.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  }
}
